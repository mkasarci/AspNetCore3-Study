<!-- 
    /Account/AccessDenied is default route of accessdenied view.
    We can change this default route by;
            
            services.ConfigureApplicationCookie(options => 
            {
                options.AccessDeniedPath = new PathString("/Administration/AccessDenied");
            });
-->
<div class="text-center">
    <h1 class="text-danger m-5">Access Denied</h1>
    <h5 class="text-danger">You don't have permission to view this resource.</h5>
</div>
